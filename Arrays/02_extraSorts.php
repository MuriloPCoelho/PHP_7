<?php 

// SPACESHIP OPERATOR (x <=> y): 
// se x < y = retorna -1
// se x > y = retorna 1
// se nenhuma das duas acontecer retorna 0


// RSORT():
// faz a mesma coisa que o sort(), só que de forma reversa
// * altera a relação entre chave e valor (ou seja, o primeiro elemento vai receber a chave do ultimo elemento após a ordenação, e caso seja um array associativo, as chaves serão substituídas por indices numéricos)

// ASORT() E ARSORT():
// desempenham a mesma função que o sort() e rsort() respectivamente, entretanto sem perder suas chaves, caso seja utilizado em um array associativo

// KSORT() E KRSORT():
// desempenham a mesma função que o sort() e rsort() respectivamente, entretanto ordena por comparação das chaves e não dos valores

// UKSORT():
// faz a mesma coisa que o usort() só que ao invés de comparar os valores irá comparar as chaves 